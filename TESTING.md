# 🧪 Testing Guide

## วิธีที่ 1: Manual Testing (แนะนำสำหรับเริ่มต้น)

### ขั้นตอนการทดสอบด้วยตนเอง

1. **เปิด VS Code ในโฟลเดอร์นี้**
   ```bash
   code .
   ```

2. **รัน Extension (Debug Mode)**
   - กด `F5` หรือไปที่เมนู `Run` → `Start Debugging`
   - จะเปิด **VS Code Extension Host** (หน้าต่างใหม่)

3. **ทดสอบ Context Menu**
   ใน VS Code Extension Host:
   
   | การทดสอบ | ขั้นตอน | ผลลัพธ์ที่คาดหวัง |
   |----------|---------|------------------|
   | คลิกขวาที่ไฟล์ .md | Explorer → คลิกขวาที่ไฟล์ .md | เห็นเมนู "Open in Browser" และ "Open in Preview" |
   | คลิกขวาที่ Editor | เปิดไฟล์ .md → คลิกขวาที่ editor | เห็นเมนู "Open in Browser" และ "Open in Preview" |
   | คลิกขวาไฟล์อื่น | คลิกขวาที่ไฟล์ .txt | **ไม่เห็น**เมนูของ Extension |

4. **ทดสอบฟีเจอร์ Open in Browser**
   - คลิกขวาที่ไฟล์ .md
   - เลือก "Open in Browser"
   - ตรวจสอบ:
     - [ ] Browser เปิดขึ้น
     - [ ] แสดงเนื้อหา Markdown ถูกต้อง
     - [ ] CSS สวยงาม
     - [ ] Dark Mode ทำงาน (ถ้า OS ตั้งค่า dark)

5. **ทดสอบฟีเจอร์ Open in Preview**
   - คลิกขวาที่ไฟล์ .md
   - เลือก "Open in Preview"
   - ตรวจสอบ:
     - [ ] เปิด Panel ด้านข้าง (Column 2)
     - [ ] แสดงเนื้อหาถูกต้อง
     - [ ] ใช้ Theme ของ VS Code
     - [ ] เปิดอีกไฟล์ → ใช้ Panel เดิม (ไม่สร้างใหม่)

---

## วิธีที่ 2: Unit Testing (Automated)

### Setup

ติดตั้ง dependencies สำหรับเทส:
```bash
npm install --save-dev @vscode/test-electron mocha @types/mocha glob @types/glob
```

### รัน Unit Tests

```bash
# รันเทสทั้งหมด
npm test

# หรือ
npm run test
```

### โครงสร้าง Test Files

```
src/
└── test/
    ├── runTest.ts           # Entry point สำหรับรันเทส
    └── suite/
        ├── index.ts         # ตั้งค่า Mocha
        └── extension.test.ts # Test cases
```

### เขียน Test เพิ่ม

```typescript
// ตัวอย่างการเขียน test
import * as assert from 'assert';
import * as vscode from 'vscode';

test('Your test name', async () => {
  // Arrange
  const expected = 'something';
  
  // Act
  const result = await someFunction();
  
  // Assert
  assert.strictEqual(result, expected);
});
```

---

## วิธีที่ 3: VS Code Extension Testing Commands

### เปิด Debug Console
- กด `Ctrl+Shift+Y` (หรือ `Cmd+Shift+Y` บน Mac)
- ดู Log ของ Extension

### ดู Developer Tools
- ใน VS Code Extension Host กด `Ctrl+Shift+I` (หรือ `Cmd+Option+I`)
- ใช้ดู Console log และ debug

### Reload Extension
- กด `Ctrl+R` (หรือ `Cmd+R`) ใน Extension Host
- หรือใช้คำสั่ง `Developer: Reload Window`

---

## 📋 Test Checklist

### Core Features
- [ ] Extension activate เมื่อเปิดไฟล์ .md
- [ ] Command "Open in Browser" ทำงานได้
- [ ] Command "Open in Preview" ทำงานได้
- [ ] Context menu แสดงเฉพาะไฟล์ .md

### Edge Cases
- [ ] ไฟล์ .md ว่างเปล่า
- [ ] ไฟล์ .md มี emoji
- [ ] ไฟล์ .md มี code block ยาวๆ
- [ ] ไฟล์ .md มีตารางใหญ่
- [ ] ไฟล์ .md มีรูปภาพ
- [ ] ไฟล์ที่ไม่ใช่ .md (ไม่ควรแสดงเมนู)

### UI/UX
- [ ] ไอคอนแสดงถูกต้อง
- [ ] ข้อความ error ชัดเจน
- [ ] การทำงานรวดเร็ว

---

## 🐛 Debugging Tips

1. **Extension ไม่ทำงาน**
   - ตรวจสอบ `out/extension.js` ถูกสร้างหรือยัง (`npm run compile`)
   - ดู Output Panel → "Log (Extension Host)"

2. **Breakpoint ไม่หยุด**
   - ต้องรันใน Debug Mode (กด F5)
   - ตั้ง breakpoint ในไฟล์ต้นฉบับ (src/extension.ts)

3. **Webview ไม่แสดง**
   - เปิด Developer Tools ดู error
   - ตรวจสอบ CSP (Content Security Policy)

---

## 📝 บันทึกผลการเทส

| วันที่ | ผู้เทส | ผลลัพธ์ | หมายเหตุ |
|--------|--------|---------|----------|
| | | | |

