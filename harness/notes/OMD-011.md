# OMD-011: CI release pipeline (GitHub Actions publish on tag)

## Status: done (2026-07-10)

## Context
Manual publishing required pasting PATs into chat (scrubbed + rotated each
time). User asked to store keys in GitHub instead. Repo is public — safe:
Actions Secrets are write-only/masked, fork PRs never receive secrets, and
the tag-push trigger is limited to users with push access.

## Design
- `.github/workflows/release.yml`: on `v*` tag → npm ci → unit tests →
  vsce package → vsce publish (VSCE_PAT) → ovsx publish (OVSX_TOKEN) →
  attach vsix to the GitHub Release.
- Actions pinned to commit SHAs (checkout@34e11487..., setup-node@49933ea...).
- Integration tests are host-only (need a live VS Code); CI gates on unit tests.
- Token issuance guide lives in docs/RELEASE.md so the human never has to
  remember the Azure DevOps / Open VSX steps again.

## Log
- 2026-07-10: Workflow + docs/RELEASE.md written. Pending: human stores the
  two secrets, then push tag v1.2.1 for the end-to-end verification (also
  completes the v1.2.1 store publishes left over from the manual flow).
- 2026-07-10: Human stored both secrets (VSCE_PAT via gh CLI, OVSX_TOKEN via
  web UI after the CLI prompt-paste failed silently). End-to-end verified:
  run 29080293963 green — unit tests, package, Marketplace + Open VSX
  publish, vsix attached to the release. Closed.
- 2026-07-10: Post-release verification — Open VSX showed 1.2.1 immediately;
  Marketplace showed 1.2.0 for ~5-10 min (async validation) before flipping
  to 1.2.1. Check live versions with `npx vsce show auttapong-tura.openmd`
  and `npx ovsx get auttapong-tura.openmd --metadata`.

## Gotchas learned
- `gh release create vX` creates the remote tag immediately; if the workflow
  lands in a later commit, pushing the tag is a no-op and CI never fires.
  Fix: delete + re-send the tag at the right commit. Deleting the tag flips
  the GitHub Release to draft — `gh release edit vX --draft=false` after.
- CI annotation: actions/checkout@v4 + setup-node@v4 target Node 20 (forced
  to 24 by the runner). Bump the pinned SHAs to v5/v6 sometime.
