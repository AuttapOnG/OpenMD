# OMD-003 — Rich Markdown rendering

**Status:** done

Acceptance criteria: harness/feature_list.json (OMD-003).

## Notes

- Shipped across v0.1.0–0.1.4. markdown-it + plugins (anchor, attrs, emoji,
  github-alerts, task-lists), Mermaid, highlight.js.
- Gotcha: hljs and Mermaid come from CDN in the browser — highlighting/diagram
  rendering must run after `window.onload`, not in Node (v0.1.2–0.1.4 fixes).
