# OMD-006 — Automated test suite

**Status:** done (2026-07-09)

Acceptance criteria: harness/feature_list.json (OMD-006).

## Notes

(Record decisions, surprises, and gotchas here while implementing.)

- mocha, @vscode/test-electron, and an src/test/ directory already exist;
  TESTING.md documents the current manual checklist to automate.
- Added `src/test/unit/render-features.test.ts` with 13 render pipeline tests:
  task-list checkboxes, heading ids, attrs id/class, server-side emoji, all
  five GitHub alert types, theme-toggle markup, copy-code script presence,
  linkify, code-fence escaping, mermaid fence handling, page title escaping,
  tables, and empty input shape.
- Fixed page title interpolation by escaping `&`, `<`, `>`, and `"` before
  inserting into `<title>`.
- Task 1 Step 2 assertion adjustments: none. All new render-feature assertions
  matched the actual plugin output; only the planned title-escaping failure was
  found in the new test file before the fix.
- Verification: after the fix, `render-features.test.js` plus `render.test.js`
  pass with 20 passing tests. The full `npm run test:unit` command reports
  20 passing and then fails in this sandbox because the existing
  `PreviewServer` tests cannot bind `127.0.0.1` (`listen EPERM`).
- Source unit test count is now 25 tests total: 13 render-feature tests, 7
  existing render tests, and 5 existing PreviewServer tests.
- 2026-07-09 (host): `npm test` integration suite verified on the host —
  5 passing (the legacy suite had 5 tests, not the 3 estimated). No fixes
  needed. `npm run test:unit` 25 passing on host. OMD-006 done.
