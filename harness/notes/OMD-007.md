# OMD-007 — Slim the repository (drop committed .vsix artifacts)

**Status:** done (2026-07-09)

Acceptance criteria: harness/feature_list.json (OMD-007).

## Notes

(Record decisions, surprises, and gotchas here while implementing.)

- 2026-07-09: Done inline (not via Codex — sandbox can't write .git). Human
  chose to keep the CURRENT release vsix tracked (installable without the
  marketplace) and explicitly approved deleting the six older ones from disk.
  Note: .gitignore already had a commented-out `*.vsix` with the same idea.
- History rewrite (filter-repo) deliberately skipped — old ~24 MB blobs remain
  in history; acceptable, avoids force-push.
