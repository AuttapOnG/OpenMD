# OMD-002 — Core open-in-browser / open-in-preview commands

**Status:** done

Acceptance criteria: harness/feature_list.json (OMD-002).

## Notes

- Shipped in v0.0.1; commands live in src/extension.ts, contributed via package.json menus.
- A Live Server / WebSocket variant was tried in v0.0.3 and reverted in v0.0.4–0.0.5
  for stability — browser open is static HTML generation.
