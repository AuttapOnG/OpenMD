# OMD-017 — Activation cleanup prefix-collision fix

**Status:** done (2026-07-13)
**Spec:** docs/specs/OMD-017-cleanup-prefix-collision.md
**Plan:** docs/plans/OMD-017-cleanup-prefix-collision.md (executed by Codex
via MCP `mcp__codex__codex`, workspace-write sandbox; reviewed + verified
+ committed by Claude)

## What changed
- `src/paths.ts`: new pure helper `isOwnVersionedDir(entry, extensionId)` —
  true only for `<extensionId>-<version>` where version is `N.N.N` with an
  optional pre-release/build suffix. No vscode imports.
- `src/extension.ts` activation cleanup: `entry.startsWith(extensionId)` →
  `isOwnVersionedDir(entry, extensionId)`. A foreign extension like
  `auttapong-tura.openmd-pro-1.0.0` no longer gets its `.temp` deleted.
- `src/test/unit/paths.test.ts`: 4 new tests (own dir, prefix collision,
  pre-release suffix, bare ID).

## Acceptance criteria
- [x] Cleanup only touches directories matching `<extensionId>-<semver>`
      exactly — regex-validated version part.
- [x] Unit test covers the prefix-collision case (extracted helper, no
      vscode imports).
- [x] Existing cleanup behavior for our own old versions unchanged (own
      versioned dirs, incl. pre-release, still match; `.temp` re-staged
      after cleanup as before).

## Verification (host)
- `npm run test:unit`: 60 passing, 0 failing (includes server tests the
  Codex sandbox could not run).
- TDD followed: red state confirmed (compile error before helper existed).

## Notes
- First delegation via Codex MCP (synchronous) instead of `codex exec`
  background — worked well for a small plan; no stdin/watchdog concerns.
