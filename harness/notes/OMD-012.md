# OMD-012: Math rendering (KaTeX) + footnotes

**Status:** done (2026-07-10)
**Spec:** docs/specs/OMD-012-katex-footnotes.md (approved in chat 2026-07-10)
**Plan:** docs/plans/2026-07-10-omd-012-katex-footnotes.md

## Approach
- Server-side KaTeX: markdown-it-texmath (delimiters: 'dollars',
  throwOnError: false) + markdown-it-footnote in createMarkdownParser().
- Browser gets only katex.min.css + 20 woff2 fonts (~320 KB), vendored by
  esbuild into media/katex/ preserving the katex/fonts/ layout so the css's
  relative url(fonts/...) resolves on all three surfaces (served /assets/,
  webview asWebviewUri, staged .temp/assets).
- Behaviors verified on host before planning: "$5 กับ $10" stays literal,
  \$ escape works, invalid TeX doesn't throw, $$ gets katex-display.
- katex 0.17.0 ships its own types; texmath/footnote declared in types.d.ts.

## Execution
Delegated to Codex CLI (codex exec --full-auto, background) per the
delegation workflow: deps preinstalled on host (Codex has no network),
Codex skips server tests (can't bind ports), host re-verifies everything
and owns commits/bookkeeping.

## Verification (2026-07-10, host)
- Automated: compile clean, 38 unit + 5 integration tests pass; vsix ls
  shows media/katex/** (commit 3ce7eee).
- Runtime (Claude /verify): booted PreviewServer with extension.ts wiring,
  drove the served page in a real browser (Playwright). Inline/display
  KaTeX render with fonts; all katex css/woff2 requests 200 from
  127.0.0.1 only (no external requests); "$5 กับ $10" stays literal;
  \$ escape ok; invalid TeX shows red error text, no crash; footnote +
  backref work; path-traversal probes on /assets/katex/** all 404
  (--path-as-is); live edit while page open → auto-refresh re-rendered
  new math server-side within ~2 s.
- Not driven: VS Code webview panel surface (same generateHtml, only
  asset URLs differ via asWebviewUri) — recommended one manual
  "Open in Preview" check.

## Follow-ups agreed during brainstorming
- OMD-013: Windows compatibility audit of ALL existing code (mirror-path
  temp files, server, cleanup) + windows-latest CI test matrix.
- OMD-014: settings — theme (light/dark/auto), server port, auto-refresh
  toggle.
