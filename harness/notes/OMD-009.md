# OMD-009 — Auto-refresh on save (preview panel + browser)

**Status:** done (2026-07-09)

Acceptance criteria: harness/feature_list.json (OMD-009). Spec: docs/specs/OMD-009-auto-refresh.md.

## Notes

(Record decisions, surprises, and gotchas here while implementing.)

- 2026-07-09: Started implementation from
  `docs/plans/2026-07-09-omd-009-auto-refresh.md`. Manual F5 smoke test
  remains assigned to the human per plan instructions.
- 2026-07-09: Implementation completed and `openmd-1.2.0.vsix` packaged at
  926K. Manual F5 smoke test is still pending, so OMD-009 remains
  `in_progress` rather than `done`.
- 2026-07-09: Unit tests that bind the local `PreviewServer` cannot complete
  in this sandbox (`listen EPERM` on `127.0.0.1`). `npm run compile` and
  `npm run build` passed; render tests pass before the PreviewServer
  before-hook hits the sandbox restriction.
- 2026-07-09: Human verified the manual smoke test — browser preview served
  from http://127.0.0.1, auto-reloads within 1-2 s on save with scroll kept;
  preview panel updates instantly. All acceptance criteria met → done.
- 2026-07-09: Full unit suite verified on the host (outside the Codex
  sandbox): 12 passing, including all 5 PreviewServer tests that hit
  `listen EPERM` in the sandbox. `npm run build` also verified on host.
- Design decision (2026-07-08): minimal localhost HTTP server with 1s mtime
  polling. file:// pages cannot fetch (browser security), so pure static
  polling is impossible; meta-refresh rejected (constant reload flicker);
  WebSocket rejected (the v0.0.3 Live Server was reverted for this complexity).
- Once this ships, the v1.0.0 mirror-path temp files only serve as the assets
  home — HTML is rendered on demand by the server.
