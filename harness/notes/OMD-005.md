# OMD-005 — v1.0.0: Mirror-path temp files and global cleanup

**Status:** done (2026-07-09)

Acceptance criteria: harness/feature_list.json (OMD-005).

## Notes

- At harness init (2026-07-07): CHANGELOG.md, package.json (version 1.0.0), and
  src/extension.ts changes are uncommitted; openmd-1.0.0.vsix (and 0.1.7) are built
  but untracked.
- 2026-07-07: committed as 614418e (single commit, includes both vsix files per
  user's choice — user opted to keep committing vsix into git for now).
- 2026-07-09: closed — v1.0.0 had in fact been on both stores since February;
  today v1.2.0 (which supersedes it) was published to VS Code Marketplace and
  Open VSX with explicit human approval, so every publish criterion is met.
